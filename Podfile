# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Altimeter' do
    
  use_frameworks!
  
  pod 'Fabric'
  pod 'Crashlytics'

end

target 'AltimeterWidget' do
    
  use_frameworks!

  # Pods for AltimeterWidget

end
